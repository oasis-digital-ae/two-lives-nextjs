[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

  # IMPORTANT: Include template files inside deployed function bundle
  included_files = [
    "netlify/functions/templates/**"
  ]